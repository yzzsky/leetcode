class Solution:
    def plusOne(self, digits):
        """
        :type digits: List[int]
        :rtype: List[int]
        """
        s = 0
        for i in digits:
            s = i+s*10
        s+=1
        s1 = list(map(int,str(s)))
        return s1
                
