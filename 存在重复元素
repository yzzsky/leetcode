class Solution:
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        s = len(nums)
        ss = len(set(nums))
        if s == ss:
            return False
        else:
            return True
