class Solution:
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        if x<0:
            return False
        
        sa = int(str(x)[::-1])
        
        if sa==x:
            return True
        else:
            return False
